/**
 * Feature flags for gating incomplete or experimental features.
 * Flip these to `true` to enable the feature.
 */
export const featureFlags = {
  /** Language selection on the Sound screen */
  languageSelection: false,
} as const;
